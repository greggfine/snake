export type Food = {
  x: number;
  y: number;
  color?: any;
  noteName?: any;
  noteFrequency?: any;
};

export type Position = {
  x: number;
  y: number;
};

export type Note = {
  note: string;
  frequency: number;
};
